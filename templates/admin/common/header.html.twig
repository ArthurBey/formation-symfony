<nav class="navbar navbar-expand-lg navbar-dark bg-primary"> <!-- bg-primary: pour différencier header admin -->
    <a class="navbar-brand" href="#">SymBNB</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-chart-line"></i> Dashboard<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_ads_index') }}"><i class="fas fa-hotel"></i> Annonces<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_bookings_index') }}" class="nav-link"><i class="fas fa-folder"></i> Réservations</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_comments_index') }}" class="nav-link"><i class="fas fa-comments"></i> Commentaires</a>
            </li>
            <li class="nav-item">
                <a href="#" class="nav-link"><i class="fas fa-users"></i> Utilisateurs</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li>
                <a href="{{ path('home') }}" target="_blank " class="nav-link"><i class="fas fa-eye"> Voir le site</i></a> {# target _blank pour ouvrir le site de base onglet #}
            </li>
            {% if app.user %}
                <li class="nav_item dropdown"> {# liste déroulante pour user connecté #}
                    <a href="" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown"> {# lien pour ouvrir la liste déroulante #}
                        <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="avatar de {{ app.user.fullName }}"> {# Rappel : app.user.ProprieteDeUser #}
                        {{app.user.fullName}}
                    </a> 

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink"> {# On spécifie ici le dropdown : dropdown-menu-right -> menu s'ouvre en étant à droite #}
                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
<!-- POUR LES MESSAGES FLASH -->
<div class="container">
    <div class="alert alert-{{label}}">
        {% for message in messages %}
            <p>{{ message | raw }}</p>
        {% endfor %}
    </div>
</div>
{% endfor %}