<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">SymBNB</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('ads_index') }}">Annonces <span class="sr-only">(current)</span></a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav_item dropdown"> {# liste déroulante pour user connecté #}
                    <a href="" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown"> {# lien pour ouvrir la liste déroulante #}
                        <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="avatar de {{ app.user.fullName }}"> {# Rappel : app.user.ProprieteDeUser #}
                        {{app.user.fullName}}
                    </a> 

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink"> {# On spécifie ici le dropdown : dropdown-menu-right -> menu s'ouvre en étant à droite #}
                        <a href="{{ path('account_index') }}" class="dropdown-item">Mon compte</a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier mon compte</a>
                        <a href="{{ path('ads_create') }}" class="dropdown-item">Publier une annonce</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('registration') }}" class="nav-link">Inscription</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('account_login') }}" class="nav-link">Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
<!-- POUR LES MESSAGES FLASH -->
<div class="container">
    <div class="alert alert-{{label}}">
        {% for message in messages %}
            <p>{{ message | raw }}</p>
        {% endfor %}
    </div>
</div>
{% endfor %}